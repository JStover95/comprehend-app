# comprehend-app Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-12-23

## Active Technologies
- TypeScript 5.x (strict mode) + AWS CDK v2, aws-cdk-lib (RDS, Lambda, Secrets Manager, IAM), pg (PostgreSQL client), @aws-sdk/rds-signer (IAM auth tokens) (002-managed-database)
- Amazon Aurora PostgreSQL 17.x (Serverless V2), Secrets Manager (master credentials) (002-managed-database)

- TypeScript (per existing `cdk/tsconfig.json`), Node.js runtime for CDK synthesis (001-base-cdk-stack)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint

## Code Style

TypeScript (per existing `cdk/tsconfig.json`), Node.js runtime for CDK synthesis: Follow standard conventions

## Recent Changes
- 002-managed-database: Added TypeScript 5.x (strict mode) + AWS CDK v2, aws-cdk-lib (RDS, Lambda, Secrets Manager, IAM), pg (PostgreSQL client), @aws-sdk/rds-signer (IAM auth tokens)

- 001-base-cdk-stack: Added TypeScript (per existing `cdk/tsconfig.json`), Node.js runtime for CDK synthesis

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
